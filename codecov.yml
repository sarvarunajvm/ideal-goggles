codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

coverage:
  precision: 2
  round: down
  range: "70...100"
  
  status:
    project:
      default:
        target: auto
        threshold: 0.2%  # Only allow 0.2% drop before failing
      backend:
        target: auto
        threshold: 0.2%
        flags:
          - backend
      frontend:
        target: auto
        threshold: 0.2%
        flags:
          - frontend
    patch:
      default:
        target: 80%      # Require 80% coverage on NEW code
        threshold: 0%

flag_management:
  default_rules:
    carryforward: true
    statuses:
      - type: project
        target: auto
        threshold: 0.2%
      - type: patch
        target: 80%

flags:
  backend:
    paths:
      - backend/
    carryforward: true
  frontend:
    paths:
      - frontend/src/
    carryforward: true
  e2e:
    paths:
      - func_tests/
    carryforward: true  # Changed to true to persist E2E results if they don't run on every commit

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: false  # Update comment even if coverage didn't change (good for visibility)
  require_base: false
  require_head: true

ignore:
  # --- Backend ---
  - "backend/**/__init__.py"           # Simplified: Ignore all __init__.py files in backend
  - "backend/src/main.py"
  - "backend/alembic/"
  - "backend/scripts/"
  - "backend/src/db/migrations/"
  - "backend/src/core/logging_config.py" # Align with pyproject.toml
  # Unused/Deprecated Backend Files (to be removed)
  - "backend/src/services/rank_fusion.py"
  - "backend/src/api/models.py"
  
  # --- Frontend ---
  - "**/*.d.ts"
  - "frontend/src/main.tsx"
  - "frontend/src/App.tsx"
  - "frontend/src/vite-env.d.ts"
  - "frontend/src/types/"
  - "frontend/src/config/"
  - "frontend/src/utils/logger.ts"       # Uses import.meta (incompatible with Jest)
  
  # UI Components (Presentational / Generated)
  - "frontend/src/components/ui/**"    # Shadcn/UI or library components
  - "frontend/src/lib/*-variants.ts"
  
  # Pure presentational components (no business logic)
  - "frontend/src/components/VirtualGrid/LoadingSkeleton.tsx"
  - "frontend/src/components/VirtualGrid/VirtualGridItem.tsx"
  - "frontend/src/components/VirtualGrid/VirtualGrid.tsx"
  - "frontend/src/components/PhotoGrid.tsx"
  - "frontend/src/components/ResultsGrid.tsx"
  - "frontend/src/components/SearchResults.tsx"
  - "frontend/src/hooks/useVirtualGrid.ts"
  - "frontend/src/components/Lightbox/LightboxNavigation.tsx"
  - "frontend/src/components/Lightbox/LightboxImage.tsx"
  - "frontend/src/components/Lightbox/LightboxMetadata.tsx"
  - "frontend/src/components/OnboardingWizard/WelcomeStep.tsx"
  - "frontend/src/components/OnboardingWizard/CompleteStep.tsx"

  # --- Tests & Build ---
  - "frontend/electron/"
  - "**/tests/"
  - "**/test_*"
  - "**/*test*"
  - "**/*spec*"
  - "**/__tests__/"
  - "**/__pycache__/"
  - "**/node_modules/"
  - "**/dist/"
  - "**/dist-electron/"
  - "**/build/"
  
  # --- Config Files ---
  - "**/*.config.*"
  - "**/.coveragerc"
  - "**/pytest.ini"